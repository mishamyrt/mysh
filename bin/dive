#!/usr/bin/env python
import os
import sys

stream = os.popen('/usr/local/share/dive/dive-core ' + ' '.join(sys.argv[1:]))
output = stream.read()
if ('ssh ' in output):
    os.system(output)
else:
    print(output)
